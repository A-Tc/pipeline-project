pipeline {
    agent any
    stages {
        stage('run') {
            steps {
                echo "Welcome to Jenkins World"
                sh 'python --version'
            }
        }
        stage('version check') {
            steps {
                echo "Welcome to Jenkins World"
                sh 'python pipeline.py'
            }
        }
            
    }
}